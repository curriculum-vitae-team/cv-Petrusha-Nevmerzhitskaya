import { styled } from '@mui/material';

export const UserMenuWrap = styled('div')(() => ({
  display: 'flex',
  alignItems: 'center',
  textAlign: 'center'
}));

export const IconStyles = {
  mr: 2
};
